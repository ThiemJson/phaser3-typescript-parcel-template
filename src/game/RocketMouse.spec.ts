function div(number1: number, number2: number): number {
  return number1 - number2;
}

function isPrimes(num: number): boolean {
  for (let i = 2; i < num / 2; i++) {
    if (num % i == 0) return false;
  }
  return num >= 2;
}

test("Minus 3 and 2 to 1", () => {
  expect(div(3, 2)).toBe(1);
});
test("Is prime 2", () => {
  expect(isPrimes(2)).toBe(true);
});

test("Minus 3 and 2 to 1", () => {
  expect(div(3, 2)).toBe(1);
});
test("Is prime 2", () => {
  expect(isPrimes(2)).toBe(true);
});

test("Minus 3 and 2 to 1", () => {
  expect(div(3, 2)).toBe(1);
});
test("Is prime 2", () => {
  expect(isPrimes(2)).toBe(true);
});

test("Minus 3 and 2 to 1", () => {
  expect(div(3, 2)).toBe(1);
});
test("Is prime 2", () => {
  expect(isPrimes(2)).toBe(true);
});

test("Minus 3 and 2 to 1", () => {
  expect(div(3, 2)).toBe(1);
});
test("Is prime 2", () => {
  expect(isPrimes(2)).toBe(true);
});

test("Minus 3 and 2 to 1", () => {
  expect(div(3, 2)).toBe(1);
});
test("Is prime 2", () => {
  expect(isPrimes(2)).toBe(true);
});

test("Minus 3 and 2 to 1", () => {
  expect(div(3, 2)).toBe(1);
});
test("Is prime 2", () => {
  expect(isPrimes(2)).toBe(true);
});

test("Minus 3 and 2 to 1", () => {
  expect(div(3, 2)).toBe(1);
});
test("Is prime 2", () => {
  expect(isPrimes(2)).toBe(true);
});

test("Minus 3 and 2 to 1", () => {
  expect(div(3, 2)).toBe(1);
});
test("Is prime 2", () => {
  expect(isPrimes(2)).toBe(true);
});
